spring:
  application:
    name: energy-analysis-api

  datasource:
    url: jdbc:postgresql://localhost:5432/energiai
    username: postgres
    password: Postgres123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080